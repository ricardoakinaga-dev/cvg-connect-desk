{
  "name": "@cvg/admin",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo build",
    "lint": "echo lint",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cvg/database": "workspace:*",
    "@cvg/events": "workspace:*",
    "@cvg/shared": "workspace:*",
    "@cvg/auth": "workspace:*",
    "@cvg/audit": "workspace:*",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
