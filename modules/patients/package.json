{
  "name": "@cvg/patients",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 2",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cvg/auth": "workspace:*",
    "@cvg/database": "workspace:*",
    "@cvg/shared": "workspace:*",
    "drizzle-orm": "0.45.2",
    "fastify": "^5.12.4"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.2.7"
  }
}
