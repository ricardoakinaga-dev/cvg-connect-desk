{
  "name": "@cvg/labels",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'labels module'",
    "build": "echo build",
    "lint": "echo lint",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cvg/database": "workspace:*",
    "@cvg/shared": "workspace:*",
    "@cvg/auth": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
