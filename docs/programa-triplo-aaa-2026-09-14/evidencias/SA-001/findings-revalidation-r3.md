# SA-001/AC2 — Revalidação corrente de A01–A10

**Candidato:** revisão `754f9badac46278e77d21de91c58eedb15e80581` + worktree; identidade corrente em [`candidate-manifest-r3.json`](candidate-manifest-r3.json).
**Data:** 15/09/2026. **Executor:** lead/Codex. **Método:** inspeção da fonte atual e dos testes produtores, sem transformar comentário em aprovação. O relatório de 14/09 permanece histórico; registros anteriores continuam `BASELINE`/`STALE`.

“Corrigido” abaixo significa que o caminho atual contém a correção observável; a requalificação com o runner, falhas negativas e crítica do item continua obrigatória. Nenhum estado terminal foi promovido por esta tabela.

| Achado | Estado corrente | Fonte atual e vínculo | Aceite preservado |
|---|---|---|---|
| A01 — hash de senha em respostas administrativas | **CORRIGIDO NO CAMINHO HTTP; REQUALIFICAÇÃO PENDENTE** | `modules/admin/src/types/index.ts:7-37` define `toPublicUser`; `modules/admin/src/presentation/http/admin.controller.ts:73-79` usa a projeção no detalhe; `modules/admin/src/application/use-cases/index.ts:68-113,125-185` retorna DTO público. O teste corrente `apps/desk-api/src/__tests__/sa-004-admin-dto.integration.test.ts:19-27` cobre GET/POST/PUT e persistência do hash sem resposta privada. | Sim — SA-004 |
| A02 — reabertura Kanban mantém conversa inativa | **CORRIGIDO NO CASO DE USO; REQUALIFICAÇÃO PENDENTE** | `modules/chat/src/application/use-cases/conversation-operations.use-case.ts:517-524,647-652` restaura `isActive`/`closedAt` no movimento transacional. `modules/kanban/src/presentation/http/kanban.controller.ts:286-302` usa `moveConversation`; `conversation.repository.ts:294-321` é helper legado fora desse caminho e não deve ser confundido com o contrato atual. | Sim — SA-005 |
| A03 — movimento Kanban persiste parcialmente | **CORRIGIDO NO CASO DE USO; REQUALIFICAÇÃO PENDENTE** | `modules/chat/src/application/use-cases/conversation-operations.use-case.ts:534-542,657-730` executa status, setor, responsável, histórico, auditoria e outbox dentro de `tx`; o controller delega em `kanban.controller.ts:286-302`. | Sim — SA-006 |
| A04 — Compose produtivo do realtime sem banco | **CORRIGIDO NA CONFIGURAÇÃO; REQUALIFICAÇÃO PENDENTE** | `docker-compose.yml:227-242` configura `DATABASE_URL`, `REDIS_URL` e dependências de `postgres`/`redis` para `realtime-service`. A inspeção de YAML não substitui boot/healthcheck do serviço real. | Sim — SA-009 |
| A05 — verificador DR aceita restauração parcial | **CORRIGIDO NO VERIFICADOR; REQUALIFICAÇÃO PENDENTE** | `infra/scripts/dr-e2e.sh:104-111` chama `dr-verify-fixture.mjs` com saída estruturada; o verificador declara comparação entidade por entidade e falha em ausência/valor/vínculo (`infra/scripts/dr-verify-fixture.mjs:9-11,37-45,140-146`). O runner real continua exigido. | Sim — SA-010 |
| A06 — atalhos da Inbox perdem contexto | **CORRIGIDO NO FLUXO DE CONTEXTO; REQUALIFICAÇÃO PENDENTE** | `apps/desk-web/src/pages/Inbox.tsx:1987-1992` envia `conversationId`; `apps/desk-web/src/hooks/useConversationContext.ts:22-42` valida/preserva URL; `Tasks.tsx:143-195`, `Notes.tsx:63-115` e `Alerts.tsx:64-110` consultam o contexto e tratam 404. | Sim — SA-008 |
| A07 — admin e início de atendimento com escrita parcial | **PARCIAL: CAMINHO PRINCIPAL CORRIGIDO; LEGADO A REQUALIFICAR** | `modules/admin/src/application/use-cases/index.ts:48-65,84-113,141-185,224-251` usa transações e substituição segura de papéis; `modules/contacts/src/application/use-cases/index.ts:64-127` cria conversa, histórico, auditoria e outbox no mesmo `tx`. `modules/admin/src/infrastructure/repositories/admin.repository.ts:66-74` ainda expõe helper legado delete-then-insert, sem consumidor encontrado na busca atual. | Sim — SA-007 |
| A08 — dashboards aceitam entradas inconsistentes | **CORRIGIDO NO CONTRATO HTTP; REQUALIFICAÇÃO PENDENTE** | `modules/dashboard/src/presentation/http/dashboard.controller.ts:56-70,98-134` usa schema, enum de `groupBy`, limites e `BadRequestError` para intervalo; falta somente reexecutar a matriz de contrato no candidato corrente. | Sim — SA-011 |
| A09 — prioridade e acabamento móveis | **PARCIAL** | `apps/desk-web/src/pages/Tasks.tsx:353-373` e `apps/desk-web/src/pages/Alerts.tsx:159-166` priorizam conteúdo acionável; `apps/desk-web/src/index.css:575-587` reordena filas no celular e `Layout.css:49-69` possui shell responsivo. Persistem ressalvas visuais: `Layout.css:61` mantém sombra profunda da sidebar e não há `@font-face` corrente em `index.css`; Notes ainda exige referência quando não há contexto (`Notes.tsx:139-155,227-240`). | Sim — SA-033–049/063 |
| A10 — lacunas de comprovação para release | **ABERTO** | `package.json:13` ainda exclui suites `aaa-*`/`production/**` e outros testes reais de `test:ci`; há job de coverage em `.github/workflows/ci.yml:121-139`, mas a matriz de release, dashboards provisionáveis, backup externo e E2E real ainda não têm prova corrente consolidada. | Sim — SA-050–062 |

## Resultado da revalidação

- Conjunto verificado: exatamente `A01` até `A10`, uma linha por achado.
- Classificação corrente: `7 CORRIGIDOS NO CAMINHO ATUAL`, `2 PARCIAIS` (A07/A09) e `1 ABERTO` (A10).
- A divergência em relação ao baseline “10 ABERTOS” é explicitamente registrada: o worktree contém implementações posteriores, mas elas não são promovidas a `DONE` nem tratadas como evidência de release sem runner, regressão e I1 atuais.
- Nenhum aceite foi removido. `programa.py validate` confirma somente a estrutura do plano; não certifica esta tabela, os testes produtores ou o produto.

