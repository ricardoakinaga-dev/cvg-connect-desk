{
  "name": "@cvg/shared",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo build",
    "lint": "echo lint",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "@cvg/database": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.12.4",
    "zod": "^4.3.6"
  }
}